<h1>Books</h1>
<% @books.each do |book| %>

<div>
  <%= image_tag 'no_image.jpg' %>
  <p>Title</p>
  <%=link_to book.title, book_path(book.id) %>
  <p>Opinion</p>
  <%= book.body %>
  <%= book.user.name %>
  
</div>

<% end %>

<%= render 'create', book: @book %>