<h1>Book detail</h1>

<div>
 <%= image_tag 'no_image.jpg' %>
 <%= @book.user.name %>
 <%= @book.title %>
 <%= @book.body %>
 <%= link_to "Edit", edit_book_path(@book.id) %>
</div>
<%= render 'create', book: @new_book %>
